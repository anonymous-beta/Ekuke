{
    "id": "geo-ip",
    "name": "IP Geolocation",
    "version": "1.0.0",
    "author": "EKUKE",
    "description": "Geo-locate IPv4 via ip-api.com (free, no key) or local MaxMind if configured",
    "input_types": ["IPv4"],
    "output_types": ["Location", "Organization", "Domain"],
    "command": "python3",
    "args": ["main.py"],
    "config_fields": [
        {"name": "proxy", "field_type": "string", "default": "", "required": false, "description": "HTTP proxy URL"},
        {"name": "local_db", "field_type": "string", "default": "", "required": false, "description": "Path to GeoLite2-City.mmdb for offline mode"}
    ]
}
