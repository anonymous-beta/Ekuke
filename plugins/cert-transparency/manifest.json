{
    "id": "cert-transparency",
    "name": "Certificate Transparency",
    "version": "1.0.0",
    "author": "EKUKE",
    "description": "Query crt.sh for subdomains and certificate issuers. Direct connection, no proxying.",
    "input_types": ["Domain"],
    "output_types": ["Domain", "Organization"],
    "command": "python3",
    "args": ["main.py"],
    "config_fields": [
        {"name": "proxy", "field_type": "string", "default": "", "required": false, "description": "HTTP proxy URL"}
    ]
}
